{% include 'header.twig' %}

<body>


<form method="POST">
    <input type="hidden" name="article_id" value="{{ article.id }}">
    <table align="center">

        <tr>

            <td>Description</td>
            <td>:</td>
            <td><input type="text" name="description" id="description" value="{{ article.description }}"></td>

        </tr>
        <tr>
            <td>Content</td>
            <td>:</td>
            <td><textarea  name="content" id="content">{{ article.content }}</textarea></td>
        </tr>
        <tr>
            <td>Category</td>
            <td>:</td>
            <td>
                <select name="category">
                    {% for category in categories %}
                        {% if category.id ==  article.category_id%}
                            <option value="{{ category.id }}" selected>{{ category.name }}</option>
                        {% else %}
                            <option value="{{ category.id }}">{{ category.name }}</option>
                        {% endif %}
                    {% endfor %}
                </select>
            </td>
        </tr>
        <tr>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
            <td><input type="submit" value="Güncelle"></td>
        </tr>
    </table>
</form>
</div>
</body>
</html>
